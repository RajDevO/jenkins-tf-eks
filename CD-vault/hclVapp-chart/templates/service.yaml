apiVersion: v1
kind: Service
metadata:
  name: {{ include "hclVapp-chart.fullname" . }}
  labels:
    {{- include "hclVapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hclVapp-chart.selectorLabels" . | nindent 4 }}
